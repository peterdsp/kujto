import Foundation
import XcodeKit

/// Principal class for the Source Editor extension. The commands are declared
/// statically in `Info.plist`, so there is no dynamic setup to do here.
final class SourceEditorExtension: NSObject, XCSourceEditorExtension {
}
